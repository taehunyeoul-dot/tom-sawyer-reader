# 인수인계 — 여기서부터 이어서 하면 됩니다

*마지막 정리: 2026-08-17*

새 대화를 시작하면 **이 파일 하나만 읽어도** 프로젝트 상태와 다음 할 일을 알 수 있게 정리했습니다.
(작업 규칙은 `CLAUDE.md`, 사용자용 설명은 `README.md` 와 `docs/사용법.md`)

---

## 1. 이게 뭔가

한국인 중급 학습자(토익 600~800)가 **영어 소설을 읽으면서 독해력을 키우는 웹 앱**입니다.
학습자의 약점은 **① 어휘 부족 ② 문장 구조 파악**, 그리고 **③ "내가 제대로 해석한 건지 유추한 건지 모르겠다"** 는 불안입니다.
이 셋을 겨냥해 만들었습니다.

- **공개 주소**: https://taehunyeoul-dot.github.io/tom-sawyer-reader/
- **저장소**: https://github.com/taehunyeoul-dot/tom-sawyer-reader (Public, `main`)
- **로컬**: `E:\team_1\100_이태훈\0_claude\7_영어소설\tom-sawyer-reader`
- 결과물은 **저장소 루트의 `index.html` 하나**(7.8MB). 설치·인터넷 없이 열리고, PWA라 휴대폰 홈 화면에 추가됩니다.

---

## 2. 지금 들어 있는 것

| 항목 | 수량 |
|---|---|
| 소설 본문 | 36장 · 5,031문장 (Mark Twain, *The Adventures of Tom Sawyer*, 1876 · 퍼블릭 도메인) |
| 어휘 사전 | 5,629개 (+ 뉴스 과학어휘 394 + 사투리 축약형 23) |
| 구문 분석 | **전 문장**에 주어·동사·의미 단위·수식 관계 (spaCy) |
| 튜터 문장 분석 | **741문장 (36장 전부)** — 의미 단위별 뜻 + 전체 번역 + "왜 어려운가" |
| 장 가이드 | 36장 전부 — 배경·인물·읽는 요령·예측 질문 |
| 이해 확인 질문 | 213문항 (36장 전부) |
| **정밀 확인** | **741문장 · 2,516항목 (36장 전부)** |
| **뉴스 튜터 분석·정밀 확인** | **기사 46건 · 1,136문장 · 3,800항목** — 기사는 짧고 완결된 글이라 **모든 문장**에 붙인다 (기자 이름·연락처·링크 안내만 제외) |
| 문법 주제 | 46개 + 예문 234개 |
| 이 소설의 문장 습관 | 18유형 |
| NASA 기사 | 46건 (매일 자동 추가, 최대 60건 보관) |

### 화면 구성 (탭 5개, 아이콘 없이 균등 배치)

`읽기` · `단어` · `문법` · `기록` · `사용법`

- **읽기** — 위쪽에서 **소설 / 뉴스** 전환. 각각 **소설책(기사) / 구조분석** 두 화면.
  **소설도 기사도** `읽기 전 → 읽기 → 읽은 후` 3단계.
- **읽은 후** 카드 순서: 이해 확인 → **정밀 확인** → 구조 연습 → 주어·동사 퀴즈 → 요약 쓰기
- **문법** — 문법 주제 / 이 소설의 문장 습관 / 주어·동사 연습

---

## 3. 핵심 설계 판단 (왜 이렇게 했는지 — 뒤집기 전에 읽을 것)

1. **전체 번역을 넣지 않는다.** 장마다 가장 어려운 문장 22개 안팎, 전체 5,031문장 중 741개(약 15%)에만 번역을 단다.
   전 문장 번역이 있으면 영어를 읽는 훈련이 안 된다. **이 비율을 더 올리지 말 것.**
2. **구조 가이드는 걷어내는 것이 목적이다.** 소설책/구조분석 2단계를 없애지 말 것. 근거는 `docs/문장구조_가이드_의견.md`.
3. **해석(번역·정밀 확인)을 본문과 같은 화면에 두지 않는다.**
   정답이 옆에 있으면 "안 봤다"고 말할 수 없다. 그래서 소설도 기사도 `읽기` 와 `읽은 후` 를 나눈다.
   - 기사 **구조분석 화면에도 한국어 뜻을 찍지 않는다.** 기사는 모든 문장에 분석이 있어서
     뜻까지 깔면 기사 전체가 번역문이 되어 버린다. 뜻은 **문장을 눌렀을 때(패널)** 와 **읽은 후**에만.
   - 소설은 장당 22문장만 분석이 있어 구조분석에 뜻이 나와도 전체 번역이 되지 않는다. 그래서 소설은 그대로 둔다.
4. **정밀 확인은 "고르는 문제"가 아니라 "쓰는 문제"다.** 4지선다는 찍기·소거법으로 뚫린다.
   직접 번역을 쓴 뒤 답이 정해진 항목과 대조해야 유추로 넘어간 곳이 드러난다.
   - **한계를 숨기지 말 것**: 틀렸다는 신호는 믿을 만하지만, 맞았다는 신호는 완전하지 않다. 앱·문서에 명시돼 있다.
5. **구조 연습(순서 맞추기)은 이해도 검사가 아니다.** 영어는 기능어 단서가 강해 뜻을 몰라도 맞출 수 있다.
   "어순 감각 훈련"이라고 정직하게 이름 붙였다 — 바꾸지 말 것.
6. **저작권**: 공개 저장소이므로 **저작권이 살아 있는 자료의 본문을 넣지 않는다.**
   - 톰 소여(1876), NASA(미국 정부 제작물) → 퍼블릭 도메인, 넣어도 됨
   - 시중 문법·어휘 교재, BBC 기사 → **넣지 않음**

---

## 4. 만드는 순서 (스크립트)

```bash
pip install spacy && python -m spacy download en_core_web_sm
```

| 스크립트 | 하는 일 |
|---|---|
| `01_parse_gutenberg.py` | 원문 → 장·문단·문장 (`book.json`) |
| `02_build_lemmas.py` | 단어 세고 원형으로 묶기 |
| `03_make_chunks.py` | 뜻풀이 맡길 단어 조각내기 → `chunks/` |
| `04_merge_vocab.py` | 뜻 + 예문 합치기 (`gloss_fix.json` 을 **맨 마지막에 덮어씀**) |
| `05_find_patterns.py` | 이 소설의 문장 습관 18유형 예문 |
| `06_parse_syntax.py` | **전 문장 구문 분석** (`syntax.json`) |
| `07_annotate.py` | 장별 어휘·커버리지·구조 태그 |
| **`08_build_reader.py`** | **모든 데이터를 템플릿에 넣어 `index.html` 생성** |
| `09_export_chapter.py` | 튜터 작업용 장 추출 |
| `10_find_grammar.py` | 문법 주제별 본문 예문 찾기 |
| `11_build_grammar.py` | 문법 해설 + 예문 합치기 (원문 대조 검증) |
| `12_fetch_nasa.py` | NASA 기사 수집 |
| `13_build_news.py` | 기사 → 문장·구문분석·어휘 |
| `14_build_checks.py` | 정밀 확인 입력 만들기(`in`) · 결과 합치기(`merge`) |
| `15_news_tutor.py` | **기사**의 튜터 분석·정밀 확인 작업 뽑기(`out`) · 합치기(`merge`) |
| `check_reader.py` | 브라우저 자동 점검 (오류·개수) — **PC 폭만 봄** |
| `check_mobile.py` | 휴대폰 폭(390px)에서 화면이 삐져나가는지 전 탭 점검 |

**디자인·기능만 고칠 때**: `reader_template.html` 만 수정 → `python scripts/08_build_reader.py` → `python scripts/check_reader.py` → **`python scripts/check_mobile.py`**
**절대 `index.html` 을 직접 고치지 말 것** (재빌드하면 사라짐)

---

## 5. 자동화

`.github/workflows/daily-news.yml` — 매일 아침 7시(KST) NASA 기사 3건 수집 → 구문분석 → 재빌드 → 배포.
수동 실행: 저장소 **Actions 탭 → Run workflow**. 실제로 돌려 성공 확인함.

> ⚠️ 봇이 커밋을 올리므로 **작업 전 `git pull --rebase` 를 먼저 할 것.**
> `sw.js`(캐시 버전)와 `index.html` 은 생성물이라 충돌하기 쉽다. 충돌 시 재빌드로 해결.

---

## 6. 남은 일 (우선순위 순)

> 2026-08-17 에 **소설 36장 전부**(튜터 분석 741문장 · 정밀 확인 2,516항목)와
> **기사 46건 전부**(1,136문장 · 3,800항목, 부속물 빼고 모든 문장)를 채웠습니다.
> 기사도 소설과 같은 3단계(읽기 전 / 읽기 / 읽은 후)로 바꿨습니다. 콘텐츠 확장은 일단락됐습니다.

1. **실제로 써 보고 다듬기** ← 지금 여기
   13장 이후는 아직 사람이 읽어본 적이 없다. 휴대폰으로 몇 장 풀어 보고
   질문이 억지스럽거나 답이 애매한 항목이 있으면 그 장의 `tutor/checks/out_NN.json` 을 고친 뒤
   `14_build_checks.py merge` → `08` 재빌드.
2. **새로 들어온 기사에 튜터 분석 채우기** ← 되풀이되는 일
   봇이 매일 기사를 넣지만 **자동 작업에는 모델이 없어** 구조분석·어휘까지만 붙는다.
   튜터 분석과 정밀 확인은 사람이 돌려야 한다. 일주일에 한 번쯤:
   ```bash
   python scripts/15_news_tutor.py out      # 분석 없는 기사만 골라 작업 생성
   # tutor/news/TUTOR_PROMPT.md 를 읽혀 기사 하나당 에이전트 하나
   python scripts/15_news_tutor.py merge
   python scripts/08_build_reader.py
   ```
   분석이 없는 기사도 **화면은 멀쩡하다** — 안내문이 뜨고 구조분석은 그대로 된다.
3. **뉴스 사전 보충** — 새 기사에 사전에 없는 단어가 `data/news_unknown.json` 에 쌓임.
   현재 159종(대부분 한 글자). 쌓이면 `tutor/news/PROMPT.md` 로 글로싱 → `data/news_gloss.json`
   (※ 글로싱용 `PROMPT.md` 와 튜터 분석용 `TUTOR_PROMPT.md` 는 다른 파일이다)
4. (선택) 어휘 확장 — 소설 어휘를 주제별로 묶거나, 공개 라이선스 목록(NGSL 등)으로 보충
5. (선택) 이해 확인 질문 늘리기 — 현재 36장에 213문항(장당 6개 안팎). 장 가이드의 `questions` 를 손보면 된다.

---

## 7. 겪은 문제와 해결 (같은 함정을 다시 밟지 말 것)

| 문제 | 원인 · 해결 |
|---|---|
| 모바일에서 화면이 축소됨 (1) | 절대 위치 역할 이름표가 오른쪽으로 삐져나가 뷰포트가 390→458px로 벌어짐. `html/.text` 에 `overflow-x:clip`. **모바일 레이아웃을 건드리면 뷰포트가 390px를 유지하는지 반드시 확인** |
| 입력칸을 누르면 화면이 **확대**됨 | 2026-08-17 발견·수정. **iOS 사파리는 글자가 16px 미만인 `input`·`textarea`·`select` 를 누르면 화면을 제멋대로 확대한다.** 기본값이 14px(장 선택은 13px)이라 번역 입력칸·요약칸·장 선택이 전부 해당됐다. **입력칸 글자를 16px 아래로 내리지 말 것.** `check_mobile.py` 가 자동으로 잡는다 (더블탭 확대는 `html{touch-action:manipulation}` 으로 이미 막혀 있음) |
| 모바일에서 화면이 축소됨 (2) — 읽은 후 화면 | 2026-08-17 발견·수정. **그리드 칸은 기본값이 `min-width:auto`라 내용의 최소 폭 아래로 줄어들지 않는다.** `.readwrap`(1fr)과 `.vlist` 의 칸이 419px·385px로 벌어져 화면이 427px가 됐다. `.readwrap>*{min-width:0}` · `.vlist>*{min-width:0}` 로 해결. **`display:grid` 를 새로 쓸 때는 자식에 `min-width:0` 을 같이 줄 것** |
| 아이폰 상단이 잘림 | `black-translucent` 상태바. 헤더에 `env(safe-area-inset-top)` 적용. 헤더가 고정 틀 안에 있어 항상 적용됨 |
| `’bout` → "한바탕/권투 시합" | 아포스트로피가 잘려 엉뚱한 단어로 연결. `data/contractions.json` |
| `reckon` 이 "영국 구어" | 뜻풀이 2,657개를 영국 배경 소설에서 재사용한 탓. `data/gloss_fix.json` 58개 정정 |
| 문법 예문 대조가 171건 실패 | 예문 표본 재추출 시 난수 상태가 밀림. 표본용 난수를 분리하고, 대조 기준을 "튜터에게 실제로 준 배치 파일"로 변경 |
| 뉴스 카드가 소설 문장을 보여줌 | 카드에 `기사id\|문단.문장` 을 저장하고 `ctxSentence()` 가 양쪽 해석 |
| 뉴스에서 모드 버튼이 안 눌림 | 버튼 연결 전에 함수가 `return` 됨. 연결을 앞으로 옮김 |
| VOA·RFA 수집 실패 | **모든 피드가 2025년 3월에 중단**, 기사 페이지·웨이백 모두 본문 없음. Wikinews 는 폐쇄 중. → NASA 로 결정 |
| 뉴스 탭인데 소설 화면이 그려짐 / 빈 화면 | 2026-08-17. **읽던 기사가 봇의 갈아 끼우기로 사라진 채 저장돼 있으면**, 상단 막대는 `S.cur.news` 로 못 찾아 단계 버튼을 안 그리고 본문은 `curArt()` 가 첫 기사로 바꿔치기해 그리다가 `#ttsPlay` 연결에서 오류 → 이전 화면(소설)이 그대로 남음. 시작 시 `tidyCur()` 가 저장 상태를 정리하고, `curArt()` 는 없으면 정직하게 `null`. **저장된 '지금 보던 곳'은 늘 지금 데이터와 대조할 것.** `check_mobile.py` 가 이 시나리오를 검사한다 |
| 의미 단위가 원문과 안 맞음 | 트웨인의 대시(—)는 **앞뒤에 공백이 없다**(`did—and`). 의미 단위 경계를 대시 위에 두면 이어 붙일 때 공백이 끼어들어 검증에 걸린다. **대시는 한 덩어리 안에 넣고 쉼표·세미콜론에서 자를 것** — 장 분석을 맡길 때 이 주의를 미리 줄 것 |
| `index.html` 이 8MB 가까이 커짐 | 기사 46건에 전 문장 튜터 분석·정밀 확인이 들어가면서 5.5MB → 7.8MB. 기사는 60건까지 쌓이므로 앞으로 더 커진다. PWA 라 **처음 한 번만 받고 그 뒤엔 캐시**되지만, 10MB를 넘기면 첫 로딩이 부담스러워진다. 그때는 (가) 보관 기사 수(`12_fetch_nasa.py` 의 `KEEP`)를 줄이거나 (나) 뉴스 데이터를 별도 파일로 빼서 필요할 때 받도록 바꿀 것 |
| `data/checks.json` 이 통째로 바뀐 것처럼 보임 | 이 파일은 **한 줄짜리 압축 JSON**이다. 들여쓰기해서 저장하면 내용이 같아도 수십만 자 차이로 잡힌다. `14_build_checks.py` 가 형식을 맞춰 준다 |

**교훈**: 외부 자료원은 **피드가 살아 있는지 + 본문이 실제로 뽑히는지 + 재배포가 되는지** 셋 다 직접 확인할 것. 검색 결과만 믿지 말 것.

---

## 8. 새 대화에서 이렇게 시작하면 됩니다

> `E:\team_1\100_이태훈\0_claude\7_영어소설\tom-sawyer-reader\HANDOFF.md` 를 읽고 이어서 작업해 줘.
> (그리고 하고 싶은 일을 말씀하시면 됩니다)

바로 확인해 볼 것:
```bash
cd "E:\team_1\100_이태훈\0_claude\7_영어소설\tom-sawyer-reader" && git pull --rebase && python scripts/check_reader.py
```
