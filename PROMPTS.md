# 뜻풀이(글로스)를 맡길 때 쓰는 프롬프트

`chunks/` 안의 조각 하나당 서브에이전트 하나. 아래에서 **파일 이름만 바꿔서** 쓴다.

```
Read <프로젝트경로>/chunks/T01.txt — each line is `단어<TAB>이 소설에서의 출현빈도`.
출처는 Mark Twain의 The Adventures of Tom Sawyer(1876, 미국 미주리주 미시시피 강변 마을 배경)이다.
19세기 미국 남부·서부 구어와 아이들 말투, 흑인·시골 사투리 철자(warn't, gwine, 'bout, kin, becuz, t'other 등)가 많다.

각 단어에 대해 정확히 한 줄씩, 아래 4개 필드를 탭(\t)으로 구분해 작성하라:

`단어<TAB>품사<TAB>한국어뜻<TAB>레벨`

- 품사: n. / v. / adj. / adv. / prep. / conj. / pron. / int. 중 해당하는 것 (여러 개면 `n., v.`처럼 쉼표)
- 한국어뜻: 이 소설 맥락에서 실제로 쓰일 핵심 의미 1~3개를 쉼표로 구분. 간결하게.
        사투리·구어 철자면 표준 철자를 괄호로 밝힐 것 (예: `warn't` → `~이 아니었다(wasn't/weren't의 사투리)`,
        `gwine` → `~할 것이다(going to의 흑인 사투리)`, `'bout` → `약, ~에 대해(about)`).
        옛말·미국 지방어·오해하기 쉬운 단어면 뜻 뒤에 괄호로 짧은 보충 (예: `(옛말)`, `(미국 남부 구어)`).
        인종 비하어(nigger 등)는 뜻을 적되 `(현대에는 쓰지 않는 심한 비하어)`를 덧붙일 것.
- 레벨: 1 = 중학~고교 기초 어휘, 2 = 수능/토익 중급, 3 = 고급·문학어·옛말·사투리·전문어
- 입력에 섞인 비단어(오타·조각난 철자)면 품사 `?`, 뜻 `비단어`, 레벨 `0`.

규칙: 입력과 같은 순서, 같은 줄 수를 지킬 것. 헤더·설명·마크다운 없이 데이터 줄만.
결과는 Write 툴로 <프로젝트경로>/gloss_raw/T01.tsv 에 저장하라. 최종 응답은 `done <줄수>` 한 줄만.
```

## 검수용 (무작위 표본)

```
<검수용 파일>은 The Adventures of Tom Sawyer 어휘 데이터에서 무작위로 뽑은 60개 표본이다. 각 줄 형식:
`단어<TAB>품사<TAB>한국어뜻<TAB>레벨(1기초/2중급/3고급)<TAB>본문빈도<TAB>예문`
각 항목의 뜻·품사·레벨이 타당한지 검수하고, 명백히 틀린 항목을 `단어: 현재뜻 → 올바른 뜻` 형식으로 보고하라.
```
