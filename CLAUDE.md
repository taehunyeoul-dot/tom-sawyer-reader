# 작업 지침 (클로드 코드용)

이 저장소는 Mark Twain의 *The Adventures of Tom Sawyer*(1876, 퍼블릭 도메인)로
**영어 독해 학습용 리더 앱**을 만드는 프로젝트다.

> **먼저 `HANDOFF.md` 를 읽을 것.** 현재 상태·남은 일·이미 겪은 함정이 거기 정리돼 있다.
> 사용자용 설명은 `README.md` 와 `docs/사용법.md`.

## 사용자에 대해

- IT 전문 용어에 익숙하지 않다. **전문어는 반드시 쉬운 말로 풀어서** 설명할 것.
- 영어 수준은 **중급(토익 600~800)**. 약점은 **어휘 부족 + 문장 구조 파악**,
  그리고 **"내가 제대로 해석한 건지 유추한 건지 모르겠다"** 는 불안.
- 결과물은 **파일 하나로 완결되는 형태**(설치·인터넷 없이 열리는 HTML)를 선호한다.
- **GitHub Pages 로 올려 휴대폰에서 학습**한다. `index.html` 은 저장소 루트에 있어야 하고 경로는 전부 상대 경로.
- 파일을 안내할 때는 **전체(절대) 경로**를 눈에 보이게 표시할 것.
- 근거를 중요하게 여긴다. 기능을 넣을 때 왜 그런지 설명하고, **한계도 숨기지 말 것.**

## 지켜야 할 원칙

1. **예문·본문은 지어내지 않는다.** 영어 문장은 반드시 `data/book.json`(소설) 또는
   `data/news.json`(NASA)에서 나와야 한다. 08·11번이 원문 대조를 자동 검증한다 — **경고 0건이어야 정상.**
2. **전체 번역을 넣지 않는다.** 장마다 가장 어려운 22개 안팎, 전체 5,031문장 중 741개(약 15%)에만 번역을 단다.
   **이 비율을 더 올리지 말 것.**
3. **구조 가이드는 걷어낼 수 있어야 한다.** 소설책 / 구조분석 2단계를 없애지 말 것
   (근거: `docs/문장구조_가이드_의견.md`).
4. **정밀 확인은 '쓰는 문제'다.** 4지선다로 바꾸지 말 것 — 찍기·소거법으로 뚫린다.
   그리고 **"이걸로 확실해진다"고 말하지 말 것.** 틀렸다는 신호만 믿을 만하다.
5. **구조 연습(순서 맞추기)은 이해도 검사가 아니다.** "어순 감각 훈련"이라는 이름을 유지할 것.
6. **저작권**: 공개 저장소다. 저작권이 살아 있는 자료(시중 교재, BBC 기사 등)의 **본문을 넣지 않는다.**
   퍼블릭 도메인(톰 소여·NASA)만 넣는다.
7. **레벨 기준**: 1 = 중고교 기초 / 2 = 수능·토익 중급 / 3 = 고급·문학어·옛말·사투리 / 0 = 비단어

## 자주 하게 될 작업

### 디자인·기능을 바꿀 때
`reader_template.html` 만 고치고 `python scripts/08_build_reader.py` → `python scripts/check_reader.py` → `python scripts/check_mobile.py`.
- **`index.html` 을 직접 고치지 말 것** (재빌드하면 사라진다)
- 데이터 자리: `/*__BOOK__*/ /*__VOCAB__*/ /*__RD__*/ /*__STRUCT__*/ /*__ANALYSES__*/ /*__GUIDES__*/ /*__SYNTAX__*/ /*__GRAMMAR__*/ /*__NEWS__*/ /*__CHECKS__*/`
- localStorage(키 `ts_reader_v1`)를 의도적으로 쓴다. 저장 구조를 바꿀 때는
  `Object.assign(def(), 저장값)` 방식을 유지해 기존 기록이 깨지지 않게 할 것.
- **모바일 레이아웃을 건드리면 뷰포트가 390px를 유지하는지 반드시 확인.**
  화면 전체가 축소되는 문제를 두 번 겪었다. `scripts/check_reader.py` 는 PC 폭(1280px)만 보므로 이 문제를 못 잡는다.
  1. 절대 위치 요소가 오른쪽으로 삐져나감
  2. **`display:grid` 자식은 기본값이 `min-width:auto`** 라 내용의 최소 폭 아래로 줄지 않는다.
     그리드를 새로 쓸 때는 **자식에 `min-width:0` 을 같이 줄 것**.
- **입력칸(`input`·`textarea`·`select`) 글자를 16px 아래로 내리지 말 것.**
  iOS 사파리가 누를 때 화면을 제멋대로 확대한다. `check_mobile.py` 가 자동으로 잡는다.

### 튜터 분석·장 가이드 (36장 전부 완료 · 741문장)
새로 만들 일은 없고 **고칠 때** 쓴다. 다시 뽑으려면:
```bash
python scripts/09_export_chapter.py 13 14 15   # tutor/work/ 에 입력 생성
```
`tutor/PROMPT.md` 를 서브에이전트에게 읽히고 장 번호만 바꿔 맡긴다(장 하나당 에이전트 하나, 병렬 가능).
결과를 `tutor/analyses_NN.json`, `tutor/guide_NN.json` 으로 저장 → `08` 재빌드.
- 분석 문장의 `en` 은 `book.json` 문장과 **글자까지 같아야** 한다.
- **대시(—)는 앞뒤에 공백이 없다.** 의미 단위 경계를 대시 위에 두지 말 것 (이어 붙일 때 공백이 끼어든다).

### 정밀 확인 (36장 전부 완료 · 741문장·2,516항목)
튜터 분석이 먼저 있어야 한다. `tutor/checks/PROMPT.md` 참고.
```bash
python scripts/14_build_checks.py in 13 14 15   # 분석 → tutor/checks/in_NN.json
# tutor/checks/PROMPT.md 로 서브에이전트 → out_NN.json 저장
python scripts/14_build_checks.py merge         # out_*.json 전부 → data/checks.json
python scripts/08_build_reader.py
```
`merge` 가 유형·빈칸을 검사하고, **확인 항목이 없는 문장을 장별로 알려 준다.** 경고 0건이어야 정상.
- 항목은 **답이 하나로 정해진 것**만. 유형은 `neg/who/pron/mod/time` 다섯 가지. 문장당 2~4개.
- `q` 안에서 `<b>` 로 감싼 영어는 **원문에 글자 그대로 있어야** 한다(줄일 때는 `…` 표시).

### 뜻풀이가 이상할 때
`data/gloss_fix.json`(04번이 최종 덮어씀) 또는 `data/contractions.json`(아포스트로피 축약형).
고친 뒤 `04 → 07 → 08` 재실행. **`data/vocab.json` 만 고치면 재빌드 때 사라진다.**

### 문법 주제를 손볼 때 (현재 46개)
`10_find_grammar.py` 의 `TOPICS` 수정 → 실행 → 예문이 5개 미만인 주제는 제외 →
`tutor/grammar/PROMPT.md` 로 해설 집필 → `11` → `08`.

### 뉴스
`12_fetch_nasa.py` → `13_build_news.py` → `08`. 매일 자동으로도 돈다(`.github/workflows/daily-news.yml`).
**작업 전 `git pull --rebase` 를 먼저 할 것** — 봇이 커밋을 올린다.

**기사에도 튜터 분석·정밀 확인이 있다** (기사 6건·100문장·336항목). 자동 작업에는 모델이 없어
새 기사에는 구조분석·어휘까지만 붙으므로, 튜터 분석은 따로 돌려야 한다.
```bash
python scripts/15_news_tutor.py out       # 분석 없는 기사만 골라 tutor/news/work/ 에 생성
# tutor/news/TUTOR_PROMPT.md 를 읽혀 기사 하나당 에이전트 하나 (분석+정밀확인을 한 번에 씀)
python scripts/15_news_tutor.py merge     # 원문 대조 검증 후 data/news_tutor.json
python scripts/08_build_reader.py
```
- 뉴스 문장 번호는 **`기사id|문단.문장`**. 소설(`장.문단.문장`)과 같은 `ANA`/`CHECKS` 에 들어가되 섞이지 않는다.
- 기사는 60건까지만 보관하므로, **없어진 기사의 분석은 08번이 알아서 뺀다.**
- 분석이 없는 기사도 화면은 정상 동작한다(안내문 + 구조분석). 이 동작을 없애지 말 것.

## 하지 말 것

- 전 문장 번역을 넣는 것
- 영어 본문을 모델이 창작하거나 고쳐 쓰는 것
- 구조 가이드를 끌 수 없게 만드는 것
- `index.html` 을 직접 고치는 것
- 저작권이 살아 있는 자료의 본문을 저장소에 넣는 것
- 외부 자료원을 검색 결과만 보고 도입하는 것
  (**피드 생존 + 본문 추출 + 재배포 가능** 셋을 직접 확인할 것. VOA·RFA·Wikinews 로 세 번 헛발질했다)
