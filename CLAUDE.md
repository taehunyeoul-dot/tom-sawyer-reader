# 작업 지침 (클로드 코드용)

이 저장소는 Mark Twain의 *The Adventures of Tom Sawyer*(1876, 퍼블릭 도메인)로
**영어 독해 학습용 리더 앱**을 만드는 프로젝트다. 전체 흐름은 `README.md` 를 먼저 읽을 것.

## 사용자에 대해

- IT 전문 용어에 익숙하지 않다. **전문어는 반드시 쉬운 말로 풀어서** 설명할 것.
- 영어 수준은 **중급(토익 600~800)**. 약점은 **어휘 부족 + 문장 구조 파악**.
- 결과물은 **파일 하나로 완결되는 형태**(설치·인터넷 없이 열리는 HTML)를 선호한다.
- 이 저장소를 **GitHub Pages 로 올려 휴대폰에서 학습**한다. 그래서 `index.html` 은 저장소 루트에 있어야 하고,
  경로는 전부 상대 경로여야 한다.
- 파일을 안내할 때는 **전체(절대) 경로**를 눈에 보이게 표시할 것.

## 지켜야 할 원칙

1. **예문·본문은 지어내지 않는다.** 반드시 `data/book.json`(= 원문)에서 기계적으로 뽑는다.
   뜻풀이·해설은 모델이 써도 되지만, 영어 문장만큼은 원문 대조가 가능해야 한다.
2. **전체 번역을 넣지 않는다.** 어려운 문장 22개 안팎만 튜터 분석에 번역을 단다.
   전 문장 번역이 있으면 영어를 읽는 훈련이 되지 않는다.
3. **구조 가이드는 걷어낼 수 있어야 한다.** 3단계(모든 문장/긴 문장만/끔)를 없애지 말 것.
   비계는 걷어내기 위해 세우는 것이다 (`docs/문장구조_가이드_의견.md`).
4. **표제어화 규칙을 함부로 늘리지 않는다.** `-er/-est/-ly` 를 추가하면
   `number→numb`, `cover→cove` 처럼 뜻이 달라지는 잘못된 묶임이 생긴다.
5. **레벨 기준 유지**: 1 = 중고교 기초 / 2 = 수능·토익 중급 / 3 = 고급·문학어·옛말·사투리 / 0 = 비단어

## 자주 하게 될 작업

### 튜터 분석·장 가이드를 다른 장으로 늘릴 때
```bash
python scripts/09_export_chapter.py 13 14 15   # tutor/work/ 에 입력 파일 생성
```
`tutor/PROMPT.md` 를 서브에이전트에게 읽히고 장 번호만 바꿔 맡긴다(장 하나당 에이전트 하나, 병렬 가능).
결과 `tutor/analyses_NN.json`, `tutor/guide_NN.json` 저장 → `python scripts/08_build_reader.py`.
- 분석 문장의 `en` 은 `data/book.json` 의 문장과 **글자까지 같아야** 한다(앱이 위치로 연결).
- 08번이 원문 일치·의미 단위 이어붙이기·주어/동사 존재를 자동 검증해 경고를 낸다. **경고 0건**이어야 정상.

### 뜻풀이를 보충할 때
```bash
python scripts/03_make_chunks.py --missing
```
`PROMPTS.md` 의 프롬프트로 조각별 서브에이전트에 맡기고 `gloss_raw/<조각>.tsv` 저장 → 04번, 07번, 08번 다시 실행.
조각 하나는 400~450단어가 적당하다(600을 넘으면 응답이 잘린다).

### 디자인·기능을 바꿀 때
`reader_template.html` 만 고치고 `python scripts/08_build_reader.py` 를 다시 돌린다.
데이터 자리: `/*__BOOK__*/ /*__VOCAB__*/ /*__RD__*/ /*__STRUCT__*/ /*__ANALYSES__*/ /*__GUIDES__*/ /*__SYNTAX__*/`
- **localStorage(키 `ts_reader_v1`)를 의도적으로 쓴다** — 간격 반복 복습·읽기 기록에 필요하다.
  저장 구조를 바꿀 때는 기존 기록이 깨지지 않게 `Object.assign(def(), 저장값)` 방식을 유지할 것.
- 고친 뒤에는 `python scripts/check_reader.py` 로 오류가 없는지 확인한다.

### 문장구조 유형을 손볼 때
`scripts/05_find_patterns.py` 의 `PATTERNS` 에 정규식을 넣고 실행해 실제 예문 수를 확인한 뒤,
`data/structures.json` 의 해당 항목을 고치고 07·08번을 다시 돌린다.
- 예문(`ex[].en`)은 반드시 `data/pattern_hits.json` 에 실제로 나온 문장을 그대로 쓸 것.
- 히트 수가 0~5회면 그 유형은 이 소설에 사실상 없는 것이니 다른 유형으로 교체한다.

### 자동 구문 분석(구조 가이드)을 손볼 때
`scripts/06_parse_syntax.py`. 역할 코드(S/V/O/C/P/A/M/R/N/Q/T/J/I/D/G/K/Z/E/X)는
`reader_template.html` 의 `ROLE`·`ROLE_LONG` 표와 **반드시 같게** 유지할 것.

## 하지 말 것

- 전 문장 번역을 넣는 것
- 본문(영어 문장)을 모델이 창작하거나 고쳐 쓰는 것
- 구조 가이드를 끌 수 없게 만드는 것
- `index.html` 을 직접 손으로 고치는 것 (재빌드하면 사라진다 — `reader_template.html` 을 고칠 것)
- 저작권이 살아 있는 소설(예: The Silkworm)의 본문을 이 저장소에 넣는 것
